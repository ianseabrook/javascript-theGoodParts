//Write a function that takes an argument and returns that argument.

function identity(a) {
    return a;
}

/* Also Correct
* var identity = function identity(a) {
*    return a;
* }
*/

identity(3); //3
