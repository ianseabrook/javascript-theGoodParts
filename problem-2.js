/*
* Write two binary functions, add and mul, that take two numbers
* and return their sum and product.
*/

function add(a, b) {
    return a + b;
}

function mul(x, y) {
    return x * y;
}

add(3, 4); // 7
mul(3, 4); // 12
